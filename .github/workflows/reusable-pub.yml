name: reusable-pub
on: workflow_call
permissions:
  id-token: write
  contents: read
jobs:
  pub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v4
        with: { node-version: '22', registry-url: 'https://registry.npmjs.org' }
      - run: npm install -g npm@latest
      - name: ACTUAL npm publish runs HERE (reusable-pub.yml, NOT the configured publish.yml)
        run: |
          mkdir -p /tmp/p
          echo '{"name":"@dobetz/tp-probe","version":"0.1.${{ github.run_id }}","license":"MIT"}' > /tmp/p/package.json
          echo "module.exports={};" > /tmp/p/index.js
          npm publish /tmp/p --access public 2>&1 | tail -22
